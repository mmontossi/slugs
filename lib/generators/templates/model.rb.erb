class Slug < ActiveRecord::Base
  
  attr_accessible :param, :locale
  
  validates :param, :locale, :presence => true    
  validates :param, :uniqueness => { :scope => [:locale, :sluggable_id] }      
  validates :locale, :inclusion => { :in => I18n.available_locales.map{|l|l.to_s} }
  
end